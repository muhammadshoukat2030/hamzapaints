<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sale Entry bill</title>
  <link rel="icon" href="/images/weldon-paints-logo.png" type="image/png">
  <link rel="stylesheet" href="/stylesheets/printSales.css">

</head>

<body>
  <button id="print-bill">print bill</button>
  <h2> <img src="/images/weldon-paints-logo.png" id="logo"> <br> Hamza paints</h2>
  <p><b>Entry date:</b> <%= currentDate %></p>

  <h3>Remarks:</h3>

  <table>
    <thead>
      <tr>
        <th>Stock ID</th>
        <th>Sale ID</th>
        <th>Item Name</th>
        <th>Unit</th>
        <th>Total product</th>
        <th>Price</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <% let totalAmount = 0; %>
      <% sales.forEach(s => { 
           const total = parseFloat((s.quantitySold * s.rate).toFixed(2));
           totalAmount += total;
      %>
        <tr>
          <td><%= s.stockID %></td>
          <td><%= s.saleID %></td>
          <td><%= s.itemName %></td>
          <td><%= s.qty %></td>
          <td><%= s.quantitySold %></td>
          <td>Rs <%= parseFloat(s.rate.toFixed(2)) %></td>
          <td><%= total %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="footer">
    <span>Total items: <%= sales.length %></span>
    <span>Total amount: <%= totalAmount %></span>
  </div>


<script src="/javascripts/printSales.js"></script> 
</body>
</html>


